# Development Profile overrides
# Run with local properties, DB updates enabled, verbose logging

spring.jpa.hibernate.ddl-auto=update

# Local dev database credentials (safe for dev only)
spring.datasource.url=${DEV_DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DEV_DB_USERNAME:postgres.qurcampttldycfctcowc}
spring.datasource.password=${DEV_DB_PASSWORD:I-SPICE2026}

# Local dev service credentials
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SMPIWzuwbaWj3R}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:4jbGS8CX31yD7Mz2dpV4S6GJ}
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
app.base-url=http://localhost:8080

# Enable verbose logging for debugging
logging.level.com.company.I_SPICE=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Disable caching for frontend dev
spring.thymeleaf.cache=false

